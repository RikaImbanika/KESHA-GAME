1) All static stripts place into S.cs field
Then in Start() write "S.MyClass = this";
Then put script on AllFather in "Start" scene.
2) All class fields name with _ and method parameters without _
3) Separate new logic to new .cs files with understandable names as much as you can
4) All other new addings make with analogy of existing ones
5) Use shaders which include exponential fog
6) Place everything according to existing folders ierarhy
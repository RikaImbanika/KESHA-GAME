COMMANDS:
=======================================================
/entities; /summon <entity> <count>;
/items; /get <item> <count>;
/tp <scene>; /scenes; /scene; /map;
/tp <x> <y>; /tp <x> <y> <z>; /spawn;
/jump <power>; /push <power>; /push <x> <y>; /push <x> <y> <z>;
/heal <amount>; /heal; /save; /checkpoint; /godmode;
/damage <amount>; /dam <amount>; /killme; /kill; /power <value>;
/help; /combinations; /coords; /scenename; /snakes;
/fog; /fog <red> <green> <blue> <d>; /fog <color> <d>;
There are also many aliases for commands.
=======================================================